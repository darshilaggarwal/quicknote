<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> <%= user.username%></title>
  <link rel="icon" type="image/x-icon" href="/illustrations/favicon.ico">

    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 ">

    <!-- Navbar -->
    <nav class="bg-zinc-900 p-5">
        <div class="max-w-screen-xl mx-auto flex justify-between items-center">
            <a href="./" class="text-white text-3xl tracking-tighter">
                QuickNote
            </a>
            <div class="flex gap-10">
                <a href="/upload" class="bg-yellow-500 text-white px-2 py-2 rounded-lg hover:bg-yellow-700 text-s">
                    Edit Profile
                </a>
                <a href="/logout" class="bg-red-500 text-white px-2 py-2 rounded-lg hover:bg-red-700 text-s">
                    Logout
                </a>
            </div>
            
        </div>
    </nav>

    <div class="bg-blue-600 items-center text-center text-white max-w-screen-xl mx-auto justify-center p-2 items-center tracking-tight"
  ></div>

  <div class="flex ml-14 mt-8 p-6  ">
    <div class="h-18 w-20 bg-red-500 rounded-lg mr-5 overflow-hidden ">
        <img class="h-18 w-20 object-cover " src="/uploads/<%= user.profilepic %>" alt="">
    </div>
    <h2 class="text-2xl tracking-tight text-white  "> <span class="text-3xl tracking-tight">Hello 👋</span>
        <br>
         <%= user.name %> </h2>
  </div>
  

    <!-- Profile Section -->
    <div class="max-w-2xl mx-auto mt-8 p-6 bg-zinc-800 shadow-lg rounded-lg" >
        

        <!-- Post Input -->
        <form action="/post" method="post" class="mt-6">
            <textarea name="content" class="w-full p-3 border border-gray-300 text-white bg-transparent rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400" rows="4" required="true" placeholder="What's on your mind?"></textarea>
            <button type="submit" class="mt-3 bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-800">
                Post
            </button>
        </form>
    </div>

    <!-- Posts Section -->
    <div class="max-w-2xl mx-auto mt-8">


        <% user.posts.reverse().forEach((post)=>{ %>

            <div class="bg-zinc-800 p-5 mb-4 shadow-lg rounded-lg">
                <h3 class="text-lg tracking-tight text-blue-600">@<%=user.username%></h3>
                <p class="text-white mt-2"><%= post.content%></p>
                <div class="mt-3 flex justify-between items-center">
                    <a href="/edit" class="text-yellow-600 ">Edit</a>
                    <button class="text-red-500 ">Like ❤️ </button>
                </div>
            </div>

      <%  })%>
       
           
            

    </div>

</body>
</html>
