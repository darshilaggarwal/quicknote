<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> <%= user.username %></title>
  <link rel="icon" type="image/x-icon" href="/illustrations/favicon.ico">

    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 ">
    
    <nav class="bg-zinc-900 p-5">
        <div class="max-w-screen-xl mx-auto flex justify-between items-center">
            <a href="./" class="text-white text-3xl tracking-tighter">
                QuickNote
            </a>
            <div class="flex gap-10">
                <a href="/profile/upload" class="bg-yellow-500 text-white px-2 py-2 rounded-lg hover:bg-yellow-700 text-s">
                    Edit Profile
                </a>
                <a href="/logout" class="bg-red-500 text-white px-2 py-2 rounded-lg hover:bg-red-700 text-s">
                    Logout
                </a>
            </div>
            
        </div>
    </nav>

    <div class="bg-blue-600 items-center text-center text-white max-w-screen-xl mx-auto justify-center p-2 items-center tracking-tight"
  ></div>

  <div class="flex items-center gap-x-5 mt-8 p-6 ml-14">
    <!-- Profile Picture -->
    <div class="h-24 w-24 rounded-lg overflow-hidden bg-gray-700">
        <img class="h-full w-full object-cover" src="/uploads/<%= user.profilepic %>" alt="Profile Picture">
    </div>

    <!-- User Info -->
    <div class="text-white">
        <h2 class="text-3xl tracking-tight">Hello 👋</h2>
        <h3 class="text-2xl"><%= user.name %></h3>
        <p class="tracking-tight text-blue-500 text-sm mt-1">@<%= user.username %></p>
    </div>
</div>

  
    <div class="max-w-2xl mx-auto mt-8 p-6 bg-zinc-800 shadow-lg rounded-lg" >
        
        <form action="/post" method="post" class="mt-6">
            <input type="text" name="title" class="w-full p-3 border border-gray-300 text-white bg-transparent rounded-lg focus:outline-none mb-5" required placeholder="Enter post title">
            <textarea name="content" class="w-full p-3 border border-gray-300 text-white bg-transparent rounded-lg focus:outline-none " rows="4" required="true" placeholder="What's on your mind?"></textarea>
            <button type="submit" class="mt-3 bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-800">
                Post
            </button>
        </form>
    </div>

    <div class="max-w-2xl mx-auto mt-8">

        <% if(user.posts.length > 0){ %>
            <div class="flex flex-wrap gap-4 mt-10">
                
                    <a class="px-4 py-2 rounded-lg hover:bg-blue-800 bg-blue-600 text-white"  href="/notes">All Notes</a>
            
            </div>
        <%  } else{  %>

        <h3 class="text-zinc-500 ml-3 mt-10 ml-10"
        >no notes yet...</h3>

        <%  } %>



       
           
            

    </div>

</body>
</html>
